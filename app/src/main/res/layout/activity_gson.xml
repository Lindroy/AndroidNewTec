<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.lin.json.GsonActivity">

    <include layout="@layout/titlebar" />

    <Button
        android:id="@+id/btn_toJavaObject"
        style="@style/MyButton"
        android:text="将Json对象转换成Java对象" />

    <Button
        android:id="@+id/btn_toJavaList"
        style="@style/MyButton"
        android:text="将Json对象转换成Java集合" />

    <Button
        android:id="@+id/btn_javaToJsonObject"
        style="@style/MyButton"
        android:text="将Java对象转换为Json对象" />

    <Button
        android:id="@+id/btn_javaToJsonArray"
        style="@style/MyButton"
        android:text="将Java集合转换成Json数组" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                android:text="原始数据"
                android:textColor="@android:color/holo_blue_light"
                android:textSize="18sp" />

            <TextView
                android:id="@+id/tv_original"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="18sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="5dp"
                android:text="转换后的数据"
                android:textColor="@android:color/holo_blue_light"
                android:textSize="18sp" />

            <TextView
                android:id="@+id/tv_last"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="18sp" />
        </LinearLayout>
    </ScrollView>

</LinearLayout>
